- name: update and install nginx exporter
  apt:
    name: prometheus-nginx-exporter 

- name: Enable nginx exporter
  systemd:
    name: prometheus-nginx-exporter 
    state: started
    daemon_reload: yes
    enabled: yes